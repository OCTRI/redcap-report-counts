{
    "name": "octri/report-counts",
    "description": "External module for generating report counts.",
    "type": "project",
    "scripts": {
      "test": "vendor/bin/phpunit --colors=always",
      "test-jenkins": "vendor/bin/phpunit --colors=never --log-junit junit/phpunit.xml"
    },
    "require": {},
    "require-dev": {
        "phpunit/phpunit": "^9",
        "phpspec/prophecy-phpunit": "^2.0"
    },
    "authors": [
        {
            "name": "OCTRI",
            "email": "octrihlp@ohsu.edu"
        }
    ],
    "version": "0.2.4"
}
